import os # usado para conversar com o sistema operacional, permite o uso do getenv
from dotenv import load_dotenv #usado para ler a pasta .env onde está a chave e o modelo da IA

load_dotenv() #habilitar o .env para esconder a chave da IA

gemini_key = Key foda #Chave da IA